import { MobileNumberValidationDirective } from './mobile-number-validation.directive';

describe('MobileNumberValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new MobileNumberValidationDirective();
    expect(directive).toBeTruthy();
  });
});
